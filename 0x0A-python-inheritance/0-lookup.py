#!/usr/bin/python3
"""Defines an object attribute lookup functions."""


def lookup(obj):
    """Return a lists of an object present attributes.
    outline the method of an object.
    """
    return (dir(obj))
